
# ハードウェア構成
- n8n クラスター
    - ollama ノード
    - n8n ノード
        - rag 環境構築ノード一式
- 7dtd 実行クライアントマシン x 2
    - MOD がプレイヤーキャラクターを直接操作するマシン ( クライアント )
    - ユーザー ( パーティーのリーダー ) が操作するマシン ( ホスト )

## 動作時の構成
- MOD 動作環境のプレイヤーキャラクターを MOD が操作する
    - MOD は意思決定プロセスの遅い経路で n8n ワークフローを呼び出し高度な判断を行う

### プレイヤーキャラクターのコントロールを MOD に渡すときの操作方法
- F8 キーで MOD によるコントロール開始

# 基本デザイン・MOD がプレイヤーキャラクターを操作するときの意思決定経路
- MOD は以下 2 つの意思決定経路を持つ
    - 早い経路
        - MOD 側であらかじめ決められたロジックにより判断を行う
            - リーダーへ追従
            - 会敵時の初回反応
    - 遅い経路
        - n8n を呼び出しモデルの判断を行動に反映させる
            - 会敵後数秒後の反応

## 早い経路
- 早い反応が求められるケースにはモデルの判断を挟まない原則とする
    - 交戦を含め、初動はすべて MOD 側の決定論ロジックで出力する
    - 例
        - プレイヤーキャラクターの至近距離に脅威が出現 → 交戦開始

### 早い経路・その後
- 初動を MOD のロジックで出力した後、状況に合わせた判断をモデルにより出力する
- MOD のロジック 100% の出力を、モデルの出力で補正していくデザインとする

## 遅い経路
- 例
    - 脅威が出現したが、相手はまだこちらに気づいていない状況
        - 先制攻撃するべきか・隠れてやり過ごすかの判断を n8n で行う
        - この判断は以下の要素を複合して行う
            - その時点でのゲーム内ワールドのエンティティの状態
            - それまでの文脈
                - 例
                    - 会敵までにユーザーが MOD に対して出した指示
                        - 交戦モード
                        - 移動モード

## 会敵の 3 類型
- 未覚醒
    - スリーパー ( 眠っている・未検知 )
- 覚醒中
    - 起きてはいるが攻撃対象がローカルプレイヤーでない
- 交戦
    - 脅威が攻撃ターゲットに自分・友軍を選択している
        - 友軍判定
            - 判定に使用する API は 7dtd_310_mod_state_audit.md 参照
            - 今はこの判定の実現の優先順位を下げる

### 3 類型の詳しい内容
- 本ドキュメントと 7dtd_310_mod_state_audit.md で表現が食い違っていれば、7dtd_310_mod_state_audit.md を真として扱う
    - 判定に使用する API についても 7dtd_310_mod_state_audit.md に記載

# 体の向き
- 脅威が存在しない状況
    - 体は移動方向に向ける
- 脅威が存在する・交戦状態
    - アクティブかつ絶対距離が一番近い脅威に体を向ける
- 脅威が存在する・相手は未覚醒
    - 体は移動方向に向ける
- 脅威が存在する・相手は覚醒中
    - ユーザーの指示が交戦モード
        - 起きている or aggro済み or 覚醒を引き起こした当人 へ体を向ける
    - ユーザーの指示が移動モード
        - 体は移動方向に向ける

## 体の向き・フォールバック
- 上記「体の向き」セクションの「脅威」に該当するエンティティを見つけられなかった場合
    - フォールバック先
        - 体は移動方向に向ける

# 照準の向き
- 交戦状態
    - アクティブかつ絶対距離が一番近い脅威に向ける

# ユーザー宣言モードの失効
- 交戦モードから移動モードへ自動的に遷移する
    - トリガー
        - 近くの脅威の覚醒値が未覚醒に落ちたら

## ユーザー宣言モード失効の例外条件
- 以下の状況では交戦モードの自動解除を行わない
    - 建物探索中
    - クエスト進行中
    - まだ未覚醒の脅威が近くに存在する
- 上記 3 条件はいずれも API で取得可能なことを確認済み
    - 建物探索中 : EntityPlayer.enteredPrefab ( POI 進入フラグ )
    - クエスト進行中 : EntityPlayer.QuestJournal.ActiveQuest
    - 未覚醒の脅威が近くに存在 : 近傍エンティティ列挙 + IsSleeper
    - いずれもリーダーの EntityPlayer から読む ( 監査ファイルへの追記を推奨 )
- この判断の実装は優先順位を下げる

# 緊急事態・ユーザー死亡時
- 防衛ホールドモードへ遷移
    - その場からなるべく動かない
    - 接近する脅威は、初期地点 ( アンカー ) 付近の範囲内で回避
    - 回避不能な脅威は排除する
    - 体の向きは最近傍脅威に向ける
    - 本 MOD による長距離移動が困難であることを見越して、ユーザー死亡時にはユーザーによる救出を待つ

## ユーザー死亡の判定条件
- leader.IsDead()
- ユーザーが回線切断で MOD 操作プレイヤーキャラクターだけワールドに取り残されたケース

## 交戦中の緊急事態発生
- 交戦モード中にユーザー死亡もしくはユーザー回線切断時
    - 防衛ホールドモードへ遷移
    - 防衛ホールドは交戦より優先する

## 防衛ホールドからの復帰 ( follow への抜け道 )
- 抜け道は以下の 2 つ
    - ユーザーの「来い」コマンド → テレポート → follow
        - 「来い」は自然言語指示を遅い経路で蒸留した離散コマンドとして受け取る
        - 実行は必ず recallGate を通す ( リセットセクション参照 )
            - 現時点では recallGate は無条件で許可を返す
        - 許可されたら、リセットセクションのテレポートでリーダー近傍へ移動し follow へ復帰する
        - 長距離の自律移動を行わない方針のため、この合流はテレポートで実現し経路探索を回避する
    - リーダー生存 & 通常追従範囲内まで接近 → follow
        - リーダーが自力で復活し ( leader.IsAlive() が true )、通常の追従範囲内まで近づいてきた場合
        - 防衛ホールドを抜けて follow へ静かに復帰する
        - リーダーが生存していても追従範囲外に遠い間は、長距離移動せず防衛ホールドを継続する

# リセット
- MOD 制御下のプレイヤーキャラクターの救出が困難なケースのため、リセット方法としてユーザー近傍にテレポートさせるオプションを用意
    - テレポートの実現手段
        - SetPosition / EntityPlayer.Teleport ( MP では NetPackageTeleportPlayer 経由 )
        - リーダー座標ちょうどではなく脇に少しオフセットし、着地地面を確認する ( 優先度低 )
    - 現時点でテレポートに制限を設けない
    - 将来的に制限を付加する予定
        - 呼び出しルールを recallGate チェックへ集約する設計とする
            - 呼び出し ( テレポート実行 ) は必ず recallGate を一度通す ( 単一チョークポイント )
            - recallGate は { bool allowed, Reason reason } を返す
                - 現時点では無条件で許可を返す
            - 呼び出し拒否の分岐を先に仮実装しておく
                - 拒否時は安全既定に倒す ( 現姿勢を維持し、ブロックしない )
                - 将来内容を追記する
            - recallGate の判断に n8n を使わない
                - このルールは MOD 側で決定論的に行う
        - 制限ルールの候補 ( 将来 )
            - クールダウン
            - 特定 POI / ゾーン内でのロックアウト
            - ホードナイト中の禁止
            - 距離バンド ( 近すぎ・遠すぎで不可 )

# 最初のターゲット
- 最小規模で「まずは最低限動く」ところの確認から始める

# 長期視点のターゲット
- 自律動作するプレイヤーキャラクター
    - 現時点ではこの目標については考えない
